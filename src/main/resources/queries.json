{
  "queryTemplates": [
    {
      "variables": [
        {
          "name": "first_name",
          "type": "string"
        }
      ],
      "template": {
        "name": "Get actors by first name",
        "sql": "select * from sakila.actor where FIRST_NAME = :first_name"
      }
    },
    {
      "variables": [],
      "template": {
        "name": "Get all actors",
        "sql": "select * from sakila.actor"
      }
    }
  ]
}